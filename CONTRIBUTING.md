## Branch strategy
- `main` is protected. Direct pushes blocked.
- Create feature branches from `main`: `git checkout -b feature/your-name`
- Open a PR against `main`. Only project maintainer (@zpollock) merges.

## PR requirements
- One approval (maintainer)
- All CI checks must pass
- No merge conflicts
